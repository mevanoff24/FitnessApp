%body
	%header
		.wrapper.clearfix

			#logo= link_to "FIT-SPIRATION", articles_path
			%nav
				- if current_user
					= link_to current_user.name, "#"
					= link_to "Add New Inspiration", new_post_path, class: "button"
				- else
					= link_to "Sign in", "#"
					= link_to "Sign Up", "#", class: "button"
